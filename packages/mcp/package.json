{
  "name": "mandate-mcp",
  "version": "0.1.2",
  "description": "MCP server for a spending authority an agent cannot argue with: preflight a payment against a policy anchored on chain, read any agent's budget and the reason behind every refusal, and spend only when the fifteen rules allow it.",
  "license": "MIT",
  "author": "Mandate",
  "homepage": "https://github.com/nickthelegend/mandate/tree/main/packages/mcp#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nickthelegend/mandate.git",
    "directory": "packages/mcp"
  },
  "bugs": {
    "url": "https://github.com/nickthelegend/mandate/issues"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "agent-payments",
    "spending-policy",
    "budget",
    "guardrails",
    "x402",
    "claude",
    "keeperhub"
  ],
  "type": "module",
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "bin": {
    "mandate-mcp": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/server.d.ts",
      "default": "./dist/server.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p tsconfig.json && chmod +x dist/cli.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ethers": "^6.17.0",
    "mandate-sdk": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  }
}
